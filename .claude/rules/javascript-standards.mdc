---
description: JavaScript 编码规范
globs: **/*.js
alwaysApply: false
---

# JavaScript 规范

## 错误处理

- 禁止空 catch；捕获后应记录并视情况传播或封装。
- 使用项目约定的日志（如 BotUtil.makeLog）记录错误与上下文。

## 异步与类型

- 优先 `async/await`，避免长 `.then()` 链；无依赖的并行用 `Promise.all` 或 `Promise.allSettled`。
- 类型与空值：使用 `ObjectUtils.isPlainObject`、`ObjectUtils.isArray` 做类型判断；合理使用可选链 `cfg?.server?.https?.enabled`。
