############################################
# OpenAI Chat Completions 协议兼容 LLM 工厂（多运营商）
############################################
# 位置：data/server_bots/{port}/openai_compat_llm.yaml
# 未配置 providers 时，若存在 baseUrl 等顶层字段，将自动视为单个运营商 openai_compat（向后兼容）

providers: []

# 示例（取消注释并填写后，该 key 将出现在 aistream.llm.Provider 下拉中）：
# providers:
#   - key: "openai-cn"
#     label: "OpenAI 兼容网关"
#     protocol: "openai"
#     baseUrl: "https://api.example.com/v1"
#     path: "/chat/completions"
#     apiKey: ""
#     authMode: "bearer"
#     model: "gpt-4o-mini"
#     temperature: 0.7
#     maxTokens: 4096
#     timeout: 360000
#     enableTools: true
#     enableStream: true
#     headers: {}
#     extraBody: {}
#     proxy: { enabled: false, url: "" }
