# ========================================
# 渲染后端配置
# ========================================
# 用于图片/网页渲染（如截图、卡片等）。可选：puppeteer、其他后端名。
# 实际生效路径：data/server_bots/{port}/renderer.yaml
# ----------------------------------------
# name: 渲染引擎名称，默认 puppeteer（基于 Chromium）
name: puppeteer
