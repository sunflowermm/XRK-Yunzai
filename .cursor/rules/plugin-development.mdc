---
description: 插件开发规范
globs: plugins/**/*.js
alwaysApply: false
---

# 插件开发

## 基类与结构

- 继承 `lib/plugins/plugin.js`；`import plugin from '../../lib/plugins/plugin.js'`。
- constructor 仅 `super({ name, dsc, event, priority, rule, ... })`；**禁止**在 constructor 内 `new Map()` 等状态容器，用**类字段**。
- 消息处理 `async fnc(e)`；回复用 `this.reply()` 或 `e.reply()`。

## 全局与工具

- 使用全局 `Bot`、`segment`；勿 `import Bot`、勿 `import { segment } from 'oicq'`。
- `FileUtils` / `ObjectUtils`；禁止业务直连 `fs`。
- 配置：`cfg` 或端口级 YAML；路径 `getServerConfigPath(port, name)`。
- **历史插件 YAML**：`import makeConfig from '../../lib/plugins/config.js'`（`config/<name>.yaml`，TRSS 兼容，**禁止删**）。
- **新插件配置**：CommonConfig（`plugins/<名>/commonconfig/`）。

## 工作流

- 优先 `this.callWorkflow(name, params, { e })`、`callWorkflows`、`executeWorkflow`。
- 低层：`this.getStream(name)` → `stream.execute(e, question, config)`。
- 定义在 `plugins/<插件名>/stream/*.js`（仅 `stream/`，非 `streams/`）。

## 资源释放

- 持有 chokidar、定时器、长连接：实现 `async destroy()`。
- 文件监视用 `HotReloadBase`，勿直接 chokidar。
